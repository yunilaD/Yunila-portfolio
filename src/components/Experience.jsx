/*
import { experience } from "../data/portfolio"

function Experience() {
    return (
        <section id="experience" className="py-24 bg-gray-950 px-6">
            <div className="max-w-3xl mx-auto">
                <h2 className="text-3xl font-bold text-white mb-2">Experience</h2>
                <div className="w-12 h-1 bg-emerald-500 mb-8"></div>
                <div className="space-y-6">
                    {experience.map((item) => (
                        <div key={item.id} className="border-l-2 border-emerald-500 pl-6">
                            <h3 className="text-white font-semibold">{item.role}</h3>
                            <p className="text-emerald-400 text-sm mb-1">{item.company} · {item.period}</p>
                            <p className="text-gray-400 text-sm leading-relaxed">{item.description}</p>
                        </div>
                    ))}
                </div>
            </div>
        </section>
    )
}

export default Experience*/
